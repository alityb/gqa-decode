[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gqa-decode-kernel"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["torch"]

[tool.setuptools.packages.find]
where = ["."]
include = ["gqa_decode*"]
